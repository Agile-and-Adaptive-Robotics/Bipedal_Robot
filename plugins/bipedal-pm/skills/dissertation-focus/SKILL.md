---
name: dissertation-focus
description: Dissertation-first status and task generation for Bolen_Dissertation (PSU, deadline Sept 2026) - placeholder audits, figure integrity checks, recent-work-to-chapters mapping, defense prep. Use when Ben asks about dissertation status/progress, what to write next, missing figures, what from recent work belongs in the dissertation, or defense prep. Never rewrites Ben's prose uninvited.
---

# dissertation-focus

PRIORITY 1 context: dissertation deadline is Sept 2026 (defense deck
`Bolen_Dissertation_Defense_20260910.pptx` exists). LaTeX lives at
`Documentation\Reports and Papers\Dissertation\` - `ProofFinal\` is the working copy
(`main.tex` + `chapters\` + `figs\`), `upload\` mirrors Overleaf. Builds go through the
**latex-overleaf skill** (EB475WS4 MiKTeX at `D:\MiKTeX`, pdflatex on PATH; easteregg2
Ghostscript at `D:\Anaconda\envs\gs\Library\bin\gswin64c.exe`). Ben commits via GitHub
Desktop; never commit yourself. Known chapter anchors: `20-methods.tex` carries the
Xi-methods figures (fig:xiFrames / xiBalance / xiWrapLoss, generated by
`Dissertation\Notes\make_xi_method_figures.m`), `CPG_spinal_section_draft.tex` is the
MuJoCo-SNS walker section.

## 1. Status scan
- Inventory `ProofFinal\chapters\*.tex` + `main.tex` + top-level `*_draft.tex`: name,
  mtime, age. The newest-mtime file is where work last happened.
- Placeholder grep per file: `\fillme`, `TODO`, `FIXME`, `XXX`, `\citep{???}`, `??`,
  `PLACEHOLDER`. Also read `CHATGPT_staging_20260925\PLACEHOLDER_AUDIT.md` and the
  staged `.tex` files there if present (another AI's staging - integrate, don't clobber).
- Read `Notes\figure_todo.md`, `CPG_DISSERTATION_UPDATE_NOTES.md`, `summary.md` at the
  Dissertation root.

## 2. Figure integrity
- Extract every `\includegraphics{...}` from all `.tex`; verify each target exists
  under `ProofFinal\figs\` (or the referenced tree). Report missing, stale (mtime older
  than its known generator), and unreferenced figure files in `CPG_airstepping_figs\`,
  `Figures\Aim1\`, `Figures\Aim2\`.
- Optional visual QA BEFORE Ben's own pass: render a PDF figure to PNG with
  `D:\MiKTeX\miktex\bin\x64\mgs.exe -dNOPAUSE -dBATCH -dSAFER -sDEVICE=png16m -r200
  -sOutputFile=<out>.png <fig>.pdf`, then run the project's zai vision tools
  (`understand_technical_diagram`) or a documents:visual-judge subagent on the PNG -
  check legibility at print size, font sizes, axis labels, sign/legend consistency.
  Advisory only; Ben judges final acceptance.

## 3. Recent work -> chapters mapping
Scan the last N days (default 7): `Code\MuJoCo_SNS\spinal\reports_20260923\`
(EXEC_SUMMARY_20260923.md), `reports_20260924\` (overnight_report_20260924.md),
`CHATGPT_REPORT.md`, git log (try `git` on PATH, else GitHub Desktop git at
`C:\Users\Ben Bolen\AppData\Local\GitHubDesktop\app-*\resources\app\git\cmd\git.exe`),
and new files in `CPG_airstepping_figs\` + `Notes\`. For every item produce a verdict:
- **INCLUDE** - target section/figure slot named, plus what's missing to place it.
- **ALREADY IN** - cite the chapter/label.
- **SKIP** - why (not dissertation material).
- **LATER** - post-defense.
Cross-check against the existing wiring (xi figures in 20-methods.tex,
CPG_spinal_section_draft.tex) so nothing is recommended twice.

## 4. Maintain Dissertation\Notes\DISS_STATUS.md
Living table: chapter/section | status | blocking item | last touched. Create if
absent; update rows in place. Never rewrite Ben's `.tex` prose uninvited; Overleaf
sync goes through the latex-overleaf skill.

## 5. Deliver
A ranked task list for the next work session, every task traced to a chapter/section,
with the single highest-leverage item called out first. Include a defense-prep lens:
slide deck state vs current chapter state, and the gaps list between them.
